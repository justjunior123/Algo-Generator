/**
 * This is a simple hello world algorithm, using strings!
 *
 * @param {string} input
 *
 * @returns {string}
 */
const description = "This algorithm takes in a string input and returns the string 'Hello ' concatenated with the input. Using strings!";

function helloWorldStrings(input) {
    return null;
}

module.exports = {
    func: helloWorldStrings,
    description: description
};
